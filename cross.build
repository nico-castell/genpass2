[binaries]
c       = [ 'ccache', arch + '-gcc' ]
cpp     = [ 'ccache', arch + '-g++' ]
ld      = arch + '-ld'
ar      = arch + '-ar'
as      = arch + '-as'
size    = arch + '-size'
objdump = arch + '-objdump'
objcopy = arch + '-objcopy'
strip   = arch + '-strip'
